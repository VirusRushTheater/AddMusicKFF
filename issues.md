== Some issues I discovered by testing ==
* When `Addmusic_sample groups.txt` has a commentary line (;), the program gets stuck in an infinite loop.
* In Linux, paths are case sensitive and some MML files don't have this regard (it just werks in Windows)